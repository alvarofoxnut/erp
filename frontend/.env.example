# Local dev: leave unset (Vite proxies /api to http://localhost:5000)
# VITE_API_URL=/api

# Vercel production: point to your Render API (required for split deploy)
# VITE_API_URL=https://your-service.onrender.com/api
